# Environment Variables for Dormitory Management System

# MongoDB Atlas Database
# Replace with your MongoDB connection string
MONGODB_URI="mongodb+srv://username:password@cluster.mongodb.net/dormitory_management?retryWrites=true&w=majority&appName=Cluster0"

# Authentication
# Generate a secure secret for production: openssl rand -base64 32
NEXTAUTH_SECRET="your-nextauth-secret-key-here"
NEXTAUTH_URL="https://your-domain.vercel.app"

# Database Mode
# Set to "false" to use MongoDB, "true" for in-memory demo mode
DEMO_MODE="false"